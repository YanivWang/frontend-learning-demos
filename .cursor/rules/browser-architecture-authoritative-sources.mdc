---
description: >-
  浏览器架构：Chromium 多进程、Blink、Compositor、GPU 进程、导航与站点隔离、渲染管线。
  以 chromium.org 为准，渲染管线补 web.dev。通用流程/原则/声明格式见 authoritative-sources-router.mdc。
alwaysApply: false
---

# 浏览器架构与渲染（权威来源）

**触发**：用户问浏览器/Chromium 整体架构与渲染管线,而非单一 CSS 属性或 JS 语义。JS 事件循环 → browser-js-runtime；V8 编译 → v8-engine；DevTools Performance 操作 → chrome-devtools。

**来源**（冲突时：架构以 Chromium 为准，渲染实践 web.dev 补充）：

| 用途 | 站点 | 入口 |
|------|------|------|
| 架构、多进程、Blink | Chromium 项目 | https://www.chromium.org |
| 渲染管线、合成、性能架构 | web.dev | https://web.dev |

**本主题特有**：

- 先讲进程/线程职责，再描述一次导航或一帧渲染的路径。
- 不把简化科普图当 Chromium 官方架构；不混淆 Blink 渲染步骤与 CSS 属性默认值（后者 → css）。

> 流程、比例原则、分级声明、通用原则与禁止：见 `authoritative-sources-router.mdc`。
