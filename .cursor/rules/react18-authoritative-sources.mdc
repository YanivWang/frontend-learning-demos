---
description: >-
  用户问 React 18：函数组件、Hooks、class 组件、createRoot、Concurrent、自动批处理、Suspense、Context（聊天问答或编辑相应代码时均适用）。
  必须先查 18.react.dev / React 18 Blog 核对后再答，附链接；不用于 React 19 新 API（Actions/use/useOptimistic 等）。
globs: apps/react18/**
alwaysApply: false
---

# React 18 学习问答（权威来源）

**触发**：用户问的是 **React 18.x** 框架用法与原理（含函数组件 Hooks、class 组件、并发特性、批处理），不要求读取当前打开的文件。

用户讨论 **useState/useEffect/useRef、createRoot、自动批处理、flushSync、lazy/Suspense、class 生命周期、Error Boundary** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅 react.dev 核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 18.react.dev 对应 Reference / Learn 页面，核对 API 语义、React 18 行为（如自动批处理范围）；React 18 新特性查官方 React 18 Blog。当前 react.dev 可作补充，但不得把 React 19 新 API 当成 React 18 能力。
2. **再对照仓库 demo**：若 `apps/react18` 有同主题文件，用于对齐示例；**demo 不能替代第 1 步**，若与官方文档冲突以 React 18 文档为准。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、版本差异或 API 边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 18.react.dev / React 18 Blog」；不补充未核对的 API 细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：以 18.react.dev 与 React 18 Blog 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| API 参考（Hooks、组件、DOM API） | React 18 官方文档 | https://18.react.dev/reference/react |
| 概念讲解、数据流、最佳实践 | React 18 Learn | https://18.react.dev/learn |
| React 18 特性说明 | React Blog | https://react.dev/blog/2022/03/29/react-v18 |
| 当前 React 文档（仅作补充，注意 19 差异） | React 官方 | https://react.dev/ |

## 路由通用原则

须同时遵守 `authoritative-sources-router.mdc`「通用原则」：核心结论仅来自本次核对的权威文档；未查到则说明不知道、不编造；用户截图/图片/附件及仓库 demo 仅作题意与示例、不作已核实依据；不得用工具 UI 或未核对材料反推规范。

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：react18-authoritative-sources.mdc ｜ 已核对：[页面标题](18.react.dev/React 18 Blog 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定性再举例**：说明 React 18 文档对 API/机制的表述，再给最小 JSX 示例（与 `apps/react18` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、版本差异或 API 边界附 Markdown 链接（18.react.dev Reference/Learn 或 React 18 Blog 具体页）；不得只贴首页。
3. **与 React 19 区分**：Actions、`useActionState`、`use()`、`useOptimistic`、ref 作为 prop 等 → 切换 react19 规则；本规则仅作对比背景。若用户未指定版本但问题是这些新 API，默认按 React 19 规则。
4. **实现细节**：Fiber/协调内部实现不臆造；以 React 18 官方文档与 React 18 Blog 对批处理、element、渲染的描述为准。
5. **查不到或文档未写明**：按「路由通用原则」说明不知道，**不编造**。

## 禁止

- **未查 18.react.dev / React 18 Blog 就给出 React 18 API/Hooks 结论**。
- 用 React 16/17 过时文档或第三方教程作为主要依据。
- 编造 Fiber 内部细节且无 react.dev 支撑。
- **凭用户上传的截图/图片/附件、仓库 demo、工具 UI 或模型推演充当已核对结论**（见「路由通用原则」）。

## 用户可追加的提问模板

> 请按项目规则：以 react.dev 为准回答 React 18 问题，附链接与最小示例。问题：……
