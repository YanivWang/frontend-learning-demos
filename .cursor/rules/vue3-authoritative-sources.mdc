---
description: >-
  用户问 Vue 3：Composition API、ref/reactive、script setup、defineModel、Pinia、Vue Router 4、Teleport/Suspense（纯聊天也算）。
  必须先查 vuejs.org 及配套生态官网核对后再答，附链接；不用于 Vue 2 Options API 或纯 JavaScript 语义。
alwaysApply: false
---

# Vue 3 学习问答（权威来源）

**触发**：用户问的是 **Vue 3.x** 框架本身（含 Composition API、`<script setup>`、Vue Router 4、Pinia），不要求读取当前打开的文件。

用户讨论 **ref/reactive、computed/watch、生命周期、组件通信、编译宏、内置组件、响应式原理（Proxy）** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅下列权威站点核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 vuejs.org / 配套生态文档对应页面，核对 API 语义、版本特性（如 3.4 defineModel、3.5 props 解构）。
2. **再对照仓库 demo**：若 `apps/vue3` 有同主题文件，用于对齐示例；**demo 不能替代第 1 步**，若与官方文档冲突以官方文档为准。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、版本边界或 API 限制附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 vuejs.org」；不补充未核对的 API 细节。

## 来源优先级（冲突时：以 vuejs.org 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| Vue 3 核心：Guide、Composition API、SFC | Vue 3 官方文档 | https://vuejs.org/ |
| Vue 3 API 参考 | Vue 3 API | https://vuejs.org/api/ |
| Vue Router（Vue 3 配套） | Vue Router 4.x | https://router.vuejs.org/ |
| 推荐状态管理 | Pinia | https://pinia.vuejs.org/ |

## 回答要求

1. **先定性再举例**：说明官方文档对 API/机制的定义，再给最小 Composition API / `<script setup>` 示例（与 `apps/vue3` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、版本边界或 API 限制附 Markdown 链接（Guide/API 具体页；Router/Pinia 对应章节）；不得只贴首页。
3. **版本标注**：涉及 3.4+ / 3.5+ 特性时注明最低版本；与 Vue 2 对比题以 Vue 3 文档为准。
4. **相邻层区分**：纯 TypeScript 类型写法 → typescript 规则；纯 JS 语义 → javascript 规则。

## 禁止

- **未查 vuejs.org 就给出 Vue 3 API/响应式结论**。
- 用 Vue 2 文档或过时 Vue 3 博客作为主要依据。
- 只给「众所周知」式总结且无链接。

## 用户可追加的提问模板

> 请按项目规则：以 vuejs.org 为准回答 Vue 3 问题，附链接与最小示例。问题：……
