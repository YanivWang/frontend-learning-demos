---
description: >-
  用户问 Chrome DevTools：面板用法、断点调试、Network/Performance/Memory、Source Maps、Lighthouse 集成、Chrome 扩展开发（纯聊天也算）。
  必须先查 developer.chrome.com 核对后再答，附链接；不用于 JS/CSS 语言语义或 V8 引擎原理。
alwaysApply: false
---

# Chrome DevTools 与扩展（权威来源）

**触发**：用户问的是 **Chrome 开发者工具的使用**，或 **基于 Chromium 的扩展/API 开发**，不要求读取当前打开的文件。

用户讨论 **DevTools 各面板操作、调试技巧、性能录制与分析界面、扩展 manifest/API** 时，**不得仅凭记忆直接作答**；须先查阅 Chrome 开发者文档核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 developer.chrome.com 对应页面，核对步骤、面板名称、选项含义是否与当前 Chrome 版本文档一致。
2. **再对照仓库 demo**：若 `apps/javascript/06-浏览器API` 等有相关演示，仅作示例补充；**demo 不能替代第 1 步**。
3. **最后组织回答**：按下方「回答要求」输出；关键步骤、面板/选项名称、版本差异或易错点附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 developer.chrome.com」；不补充未核对的 UI 细节。

## 来源优先级

| 用途 | 站点 | 入口 |
|------|------|------|
| DevTools 面板、调试、性能录制界面 | Chrome 开发者文档 | https://developer.chrome.com |
| Chrome 扩展开发（Manifest、API） | Chrome 开发者文档 | https://developer.chrome.com/docs/extensions |

## 回答要求

1. **先说明入口与面板**：文档中的菜单路径 / 快捷键 / 面板名称，再写操作步骤。
2. **必须标注出处**：关键步骤、面板/选项名称、版本差异或易错点附 Markdown 链接（具体 DevTools 或 Extensions 文档页）；不得只贴首页。
3. **与性能指标区分**：若用户问 LCP/INP/CLS 含义或优化手段，主规则切换至 web-performance；DevTools 仅说明「如何在该面板查看」。
4. **版本差异**：文档与本地 Chrome 不一致时注明「以实现/文档为准」。

## 禁止

- **未查 developer.chrome.com 就给出 DevTools 操作步骤**。
- 用 MDN 或博客截图教程替代 Chrome 官方文档作为首要依据。
- 编造已更名/已移除的面板或菜单项。

## 用户可追加的提问模板

> 请按项目规则：以 developer.chrome.com 为准回答 DevTools/扩展问题，附链接与步骤。问题：……
