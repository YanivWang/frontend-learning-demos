---
description: >-
  用户问 TypeScript：类型系统、泛型、收窄、Utility Types、tsconfig、声明文件、与 Vue/React 组件类型（聊天问答或编辑相应代码时均适用）。
  必须先查 typescriptlang.org 核对后再答，附链接；纯 JavaScript 运行时语义见 javascript 规则。
globs: apps/typescript/**
alwaysApply: false
---

# TypeScript 学习问答（权威来源）

**触发**：用户问的是 **TypeScript 语言与类型系统**（含 tsconfig、声明文件、框架中的 TS 用法），不要求读取当前打开的文件。

用户讨论 **类型注解/推断、interface vs type、泛型、条件类型、映射类型、strict 选项、.d.ts** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅 typescriptlang.org 核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 TypeScript Handbook / TSConfig / Release Notes 对应页面，核对类型规则、编译选项、版本特性。
2. **再对照仓库 demo**：若 `apps/typescript` 有同主题文件，用于对齐示例；**demo 不能替代第 1 步**，若与官方文档冲突以官方文档为准。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、版本/编译选项或类型边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 typescriptlang.org」；不补充未核对的类型规则细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：以 Handbook 为准，TSConfig 用于编译选项）

| 用途 | 站点 | 入口 |
|------|------|------|
| 类型系统、语法、最佳实践 | TypeScript Handbook | https://www.typescriptlang.org/docs/handbook/intro.html |
| tsconfig 编译选项 | TSConfig Reference | https://www.typescriptlang.org/tsconfig/ |
| 版本新增特性 | Release Notes | https://www.typescriptlang.org/docs/handbook/release-notes/overview.html |
| 运行时 JS 行为（补充） | MDN | https://developer.mozilla.org/en-US/docs/Web/JavaScript |

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：typescript-authoritative-sources.mdc ｜ 已核对：[页面标题](typescriptlang.org 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定性再举例**：说明 Handbook 对类型/语法的定义，再给最小 TS 片段（与 `apps/typescript` demo 风格一致）；必要时说明编译后 JS 行为。
2. **必须标注出处**：核心结论、易错点、版本/编译选项或类型边界附 Markdown 链接（Handbook 具体章节；TSConfig 具体选项页；Release Notes 对应版本）；不得只贴首页。
3. **框架交叉**：Vue 组件 props/emits 类型 → 同时查 vue3 规则中 TypeScript 章节；React 组件 props → react.dev TypeScript 页 + 本规则。
4. **查不到或文档未写明**：明确写「文档未覆盖 / 编译器版本相关」，不编造。

## 禁止

- **未查 typescriptlang.org 就给出 TS 类型系统结论**。
- 把 DefinitelyTyped 或博客类型技巧当作语言规范的首要依据。
- 混淆「TS 编译期报错」与「JS 运行时行为」而不加说明。

## 用户可追加的提问模板

> 请按项目规则：以 typescriptlang.org 为准回答 TypeScript 问题，附链接与最小示例。问题：……
