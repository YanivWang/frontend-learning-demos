---
description: >-
  用户问浏览器如何执行 JavaScript：事件循环、宏/微任务、调用栈、主线程、Web API 调度、渲染与 JS 协作（聊天问答或编辑相应代码时均适用）。
  必须先查 MDN 核对后再答，涉及标准术语查 WHATWG HTML，流程讲解可补 web.dev，附链接；不用于 V8 JIT/GC 底层或 DevTools 操作教程。
globs: apps/javascript/04-ES6+/异步/**
alwaysApply: false
---

# 浏览器 JS 运行时（权威来源）

**触发**：用户问的是 **浏览器环境中 JavaScript 如何被调度与执行**（非 ECMAScript 语法本身，也非 V8 引擎实现细节）。

用户讨论 **事件循环、任务队列、宏任务/微任务、调用栈、主线程阻塞、requestAnimationFrame、Web API 与 JS 引擎分工** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅下列权威站点核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 MDN 对应页面，核对关键结论（调度顺序、与渲染帧关系、常见误区）。涉及标准术语、任务模型或争议边界时再查 WHATWG HTML；流程讲解与图示可补 web.dev。
2. **再对照仓库 demo**：若 `apps/javascript`（如 `06-浏览器API`、`04-ES6+/异步/Promise` 中与事件循环相关文件）有同主题 demo，用于对齐示例；**demo 不能替代第 1 步**。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、调度顺序或标准边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 MDN / WHATWG HTML / web.dev」；不补充未核对的细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：标准术语以 WHATWG HTML 为准，API 语义以 MDN 为准，讲解以 web.dev 补充）

| 用途 | 站点 | 入口 |
|------|------|------|
| Event loop、任务来源、Web API 语义 | MDN | https://developer.mozilla.org/en-US/docs/Web/JavaScript |
| 微任务、任务队列、Promise 调度 | MDN Microtask Guide | https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide |
| 事件循环标准术语、task/microtask 模型 | WHATWG HTML | https://html.spec.whatwg.org/multipage/webappapis.html#event-loops |
| 浏览器执行模型、主线程、渲染协作 | web.dev | https://web.dev |

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：browser-js-runtime-authoritative-sources.mdc ｜ 已核对：[页面标题](MDN/WHATWG HTML/web.dev 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定性再举例**：说明「浏览器文档里的调度顺序/线程模型」，再给最小示例（可与 `apps/javascript` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、调度顺序或标准边界附 Markdown 链接（MDN 具体页；必要时 WHATWG HTML、web.dev 对应文章/章节）；不得只贴首页。
3. **分层说明**：若用户追问引擎层（JIT/GC），说明需切换至 V8 规则；若追问 DevTools 如何观察，切换至 chrome-devtools 规则。
4. **术语边界**：中文常说的「宏任务」不是 WHATWG HTML 的正式术语，标准语境优先写 task；可说明「宏任务」是教学俗称。
5. **查不到或文档未写明**：明确写「文档未覆盖 / 实现相关」，不编造。

## 禁止

- **未查 MDN，或涉及标准边界未查 WHATWG HTML，就给出事件循环/主线程结论**。
- 把 V8 博客或 Chromium 架构文档当作事件循环首要依据（层次不对）。
- 只给「众所周知」式总结且无链接。

## 用户可追加的提问模板

> 请按项目规则：以 MDN / web.dev 说明浏览器 JS 执行与事件循环，附链接与最小示例。问题：……
