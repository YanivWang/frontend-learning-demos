---
description: >-
  前端学习问答总路由：根据用户问题主题自动选用对应权威来源规则（JS/CSS/框架/TS/浏览器/DevTools/性能等）。
alwaysApply: true
---

# 权威来源规则路由

用户提问 **前端语言、框架、浏览器、工具、性能** 等学习类问题时，**先按主题匹配下表**，再读取对应 `.cursor/rules/*-authoritative-sources.mdc` 并按该规则作答（先查权威站、附链接、再对照仓库 demo）。

**编辑代码时同样适用**：在 `apps/<主题>` 目录下读写代码时，对应子规则会按 `globs` 自动注入；即便未自动注入，也须按本路由选规则、核对官网并附链接后再改动或解释代码。

## 强制执行

1. **必须先选规则**：只要问题属于下表任一主题，内部先选定对应规则；必要时再向用户简短说明，不要直接凭记忆回答。
2. **必须读取规则文件**：匹配到的 `*-authoritative-sources.mdc` 是本次回答的执行标准；跨主题时读取多个规则。
3. **必须实际核对官网（须真调用联网工具）**：回答核心结论、易错点、版本/兼容性或配置边界前，**必须实际发起一次联网检索/网页抓取**核对官方具体页面；**未真正调用联网工具时，禁止写「已核对」**，只能写「未实时核对（凭记忆）」，且不得编造链接或原话。不得只引用首页、仓库 demo 或模型记忆；联网确实不可用时按对应规则的「无法联网」条款降级。
4. **必须附具体链接**：核心结论、易错点、版本/兼容性或配置边界附本次核对过的官方具体页面链接；无法联网时，按对应规则的「无法联网」条款降级回答。
5. **用户指定来源优先**：用户明确指定某个官方站点或版本时，以用户指定为准；若指定来源与主题不匹配，先说明边界。
6. **比例原则**：对极基础且稳定的事实（如基本语法、默认行为）可直接作答并附权威链接，不必每次实时检索；但凡涉及版本/兼容性/边界行为或配置项，必须按第 3 条实时核对。
7. **必须先声明命中规则与已核对来源（含官网原话）**：凡命中下表任一主题，或在 `apps/` 代码场景下作答时，回答的**第一行**先输出 `命中规则：<规则文件名> ｜ 已核对：[页面标题](链接) —「<官网原话片段>」`；其中链接与原话必须摘自**本次实际联网抓取**的具体页面（非首页、非记忆）。跨多层时规则部分写成 `<主规则>（主）＋<补充规则>（补）`，原话附主规则本次核对页。**未真正调用联网工具核对时**写 `已核对：未实时核对（凭记忆）`，不得编造链接或原话；若判定为非前端学习问答、不套用本路由，则写 `命中规则：无（非前端学习问答）`。声明之后再开始正文。

| 用户问题主题 | 选用规则文件 |
|-------------|-------------|
| JavaScript 语法、API、闭包、原型、Promise、ES 特性 | `javascript-authoritative-sources.mdc` |
| CSS 属性、选择器、布局、层叠、动画、响应式 | `css-authoritative-sources.mdc` |
| TypeScript 类型系统、泛型、tsconfig、声明文件 | `typescript-authoritative-sources.mdc` |
| HTML 语义、DOM、事件、Fetch、Storage、Canvas、History、URL、Web Components | `browser-web-apis-authoritative-sources.mdc` |
| Vue 2：Options API、Vue.set、Vue Router 3、Vuex | `vue2-authoritative-sources.mdc` |
| Vue 3：Composition API、script setup、Pinia、Router 4 | `vue3-authoritative-sources.mdc` |
| React 18：Hooks、class 组件、createRoot、Concurrent、批处理 | `react18-authoritative-sources.mdc` |
| React 19：Actions、useActionState、useOptimistic、use()、RSC 边界 | `react19-authoritative-sources.mdc` |
| 浏览器里 JS 怎么跑：事件循环、宏/微任务、主线程、Web API 调度 | `browser-js-runtime-authoritative-sources.mdc` |
| Chrome DevTools、调试技巧、Performance/Network 面板、Chrome 扩展 | `chrome-devtools-authoritative-sources.mdc` |
| 网页性能优化、Core Web Vitals（LCP/INP/CLS）、加载与渲染性能 | `web-performance-authoritative-sources.mdc` |
| V8 引擎底层：JIT、GC、Ignition/TurboFan、字节码 | `v8-engine-authoritative-sources.mdc` |
| 浏览器架构：多进程、Blink、渲染管线、Compositor | `browser-architecture-authoritative-sources.mdc` |
| 前端工具链：Vite、Webpack、Rollup、Babel、ESLint、Prettier、npm/pnpm/yarn、Node 配置 | `frontend-tooling-authoritative-sources.mdc` |

## 边界说明

- **语言语义** → JS / TS / CSS 规则；**浏览器 API 与 HTML** → browser-web-apis；**框架 API** → 对应 vue2/vue3/react18/react19 规则。
- **Vue 2 vs Vue 3**：版本明确则按版本；对比题以用户指定的版本为主规则，另一版本作补充。
- **React 18 vs React 19**：通用 Hooks/渲染 → react18；Actions、`use()`、`useOptimistic` 等 19 新 API → react19。
- **TS + 框架**：类型写法 → typescript 为主；框架 props/emits/组件 API → 框架规则补充。
- **DevTools 操作** → chrome-devtools；**用 DevTools 做性能分析** → 同时参考 web-performance。
- **事件循环** → browser-js-runtime；**V8 编译** → v8-engine。
- **构建/包管理/格式化/静态检查** → frontend-tooling；框架脚手架中的框架 API 仍按框架规则。
- 一题跨多层时：按「用户主要想问哪一层」选主规则，其它层作补充并分别附链接。
- 无法匹配或非前端学习问答：不强行套用；用户明确要求某站点时以用户为准。
