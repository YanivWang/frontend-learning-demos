---
description: >-
  用户问 React 19：Actions、useActionState、useFormStatus、useOptimistic、use()、ref 作为 prop、RSC/Server Actions 面试边界（纯聊天也算）。
  必须先查 react.dev 核对后再答，附链接；通用 Hooks/渲染基础见 react18 规则。
alwaysApply: false
---

# React 19 学习问答（权威来源）

**触发**：用户问的是 **React 19.x 新增或变更的 API 与工程实践**，或 **React 19 相对 18 的差异**，不要求读取当前打开的文件。

用户讨论 **form action、useActionState、useFormStatus、useOptimistic、use() 读 Promise/Context、ref 作为 prop、Server Components/Actions 边界** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅 react.dev 核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 react.dev 对应 Reference / Blog / Upgrade Guide，核对 React 19 特性、迁移说明与 API 签名。
2. **再对照仓库 demo**：若 `apps/react19` 有同主题文件，用于对齐示例；**demo 不能替代第 1 步**，若与 react.dev 冲突以 react.dev 为准。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、版本差异或 API 边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 react.dev」；不补充未核对的 API 细节。

## 来源优先级（冲突时：以 react.dev 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| React 19 新 API 参考 | React Reference | https://react.dev/reference/react |
| React DOM / 表单相关 API | React DOM Reference | https://react.dev/reference/react-dom |
| React 19 发布说明与升级指南 | React Blog | https://react.dev/blog/2024/12/05/react-19 |
| 从 18 升级到 19 | Upgrade Guide | https://react.dev/blog/2024/04/25/react-19-upgrade-guide |

## 回答要求

1. **先说明相对 18 的变化**：引用官方 Upgrade Guide / Blog，再给最小可运行示例（与 `apps/react19` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、版本差异或 API 边界附 Markdown 链接（Reference 具体 API 页；Blog 具体章节）；不得只贴首页。
3. **与 React 18 协作**：通用 Hooks、createRoot、批处理等基础 → react18 规则；本规则聚焦 19 新增/变更。
4. **RSC/Server Actions**：以 react.dev 概念边界为准，不编造框架无关的实现细节。

## 禁止

- **未查 react.dev 就给出 React 19 新 API 结论**。
- 把 React 18 文档当作 React 19 Actions / `use()` 的首要依据。
- 混淆 Next.js 文档与 React 核心 API（除非用户明确问 Next.js 集成）。

## 用户可追加的提问模板

> 请按项目规则：以 react.dev 为准回答 React 19 问题，附链接与最小示例。问题：……
