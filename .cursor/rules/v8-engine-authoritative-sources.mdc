---
description: >-
  用户问 V8/JS 引擎底层：JIT、Ignition/TurboFan、GC、隐藏类、字节码、Sparkplug、引擎优化与去优化（纯聊天也算）。
  必须先查 v8.dev 核对后再答，附链接；不用于事件循环教程或 DevTools 面板操作。
alwaysApply: false
---

# V8 引擎底层（权威来源）

**触发**：用户问的是 **JavaScript 引擎（主要是 V8）如何实现与优化代码**，而非 ECMAScript 语言语义或浏览器 DevTools 用法。

用户讨论 **JIT 编译、垃圾回收、inline cache、隐藏类、字节码、优化/去优化（bailout）** 时，**不得仅凭记忆直接作答**；须先查阅 V8 官方材料核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 v8.dev（博客/docs）对应页面，核对机制描述、术语、时间线是否与官方一致。
2. **再对照仓库 demo**：本仓库若无 V8 专项 demo，可给最小 JS 片段说明现象；**不得以 demo 替代第 1 步**。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、实现机制、版本演进或易错点附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 v8.dev」；不补充未核对的实现细节。

## 来源优先级（冲突时：以 v8.dev 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| 引擎架构、JIT、GC、优化机制 | V8 官方 | https://v8.dev/blog |
| V8 文档与特性说明 | V8 官方 | https://v8.dev/docs |

## 回答要求

1. **先机制后示例**：用官方术语说明原理，再给可观察的 JS 层示例（如性能差异、deopt 场景——若文档有说明）。
2. **必须标注出处**：核心结论、实现机制、版本演进或易错点附 Markdown 链接（v8.dev 具体文章/文档页）；不得只贴首页。
3. **与上层区分**：事件循环、主线程调度 → browser-js-runtime；浏览器多进程 → browser-architecture。
4. **实现演进**：V8 版本间机制可能变化，注明文章日期或「以当前 v8.dev 为准」。

## 禁止

- **未查 v8.dev 就给出 JIT/GC/隐藏类等实现结论**。
- 把多年前的引擎科普文章当作 V8 当前实现的首要依据。
- 主动贴 V8 C++ 源码大块（用户未明确要求时不引用）。

## 用户可追加的提问模板

> 请按项目规则：以 v8.dev 为准回答引擎底层问题，附链接。问题：……
