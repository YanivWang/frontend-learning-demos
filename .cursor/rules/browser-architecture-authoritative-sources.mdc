---
description: >-
  用户问浏览器架构：Chromium 多进程、Blink 渲染引擎、Compositor、GPU 进程、导航与站点隔离、渲染管线（纯聊天也算）。
  必须先查 chromium.org / web.dev 核对后再答，附链接；不用于 CSS 属性用法或 V8 JIT 细节。
alwaysApply: false
---

# 浏览器架构与渲染（权威来源）

**触发**：用户问的是 **浏览器/Chromium 整体架构与渲染管线**，而非单一 CSS 属性或 JS 语言语义。

用户讨论 **多进程模型、Browser/Renderer/GPU 进程、Blink、Compositor Thread、光栅化、站点隔离、从 URL 到像素** 时，**不得仅凭记忆直接作答**；须先查阅下列权威站点核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 Chromium / web.dev 对应页面，核对架构组件名称、数据流是否与官方一致。架构与进程模型优先 Chromium；渲染管线入门与图示可补充 web.dev。
2. **再对照仓库 demo**：若 `apps/css/06-性能`（合成层、重排重绘）有相关内容，作现象层补充；**demo 不能替代第 1 步**。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、架构组件、数据流或版本实现差异附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 Chromium / web.dev」；不补充未核对的架构细节。

## 来源优先级（冲突时：架构以 Chromium 为准，渲染实践以 web.dev 补充）

| 用途 | 站点 | 入口 |
|------|------|------|
| Chromium 架构、多进程、Blink | Chromium 项目 | https://www.chromium.org |
| 渲染管线、合成、性能相关架构讲解 | web.dev | https://web.dev |

## 回答要求

1. **先架构后数据流**：说明有哪些进程/线程、职责分工，再描述一次导航或一帧渲染的路径。
2. **必须标注出处**：核心结论、架构组件、数据流或版本实现差异附 Markdown 链接（Chromium 具体文档；web.dev 对应文章）；不得只贴首页。
3. **与相邻层区分**：JS 事件循环 → browser-js-runtime；V8 编译 → v8-engine；DevTools Performance 面板操作 → chrome-devtools。
4. **查不到或文档未写明**：明确写「文档未覆盖 / 版本实现差异」，不编造。

## 禁止

- **未查 Chromium / web.dev 就给出多进程/渲染管线结论**。
- 把简化科普图当作 Chromium 官方架构的首要依据。
- 混淆 Blink 渲染步骤与 CSS 属性默认值（后者属 css 规则）。

## 用户可追加的提问模板

> 请按项目规则：以 Chromium / web.dev 为准回答浏览器架构问题，附链接。问题：……
