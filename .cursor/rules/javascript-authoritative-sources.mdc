---
description: >-
  用户提问 JavaScript 语言语义、API、闭包/原型/Promise/模块/ES 特性等（纯聊天、未打开文件也算）。
  必须先查 MDN 核对后再答，概念教学可补查 javascript.info，附具体链接；浏览器事件循环/V8/DevTools/CSS/Vue/React/TypeScript 走对应规则。
alwaysApply: false
---

# JavaScript 学习问答（权威来源）

**触发**：用户问的是 JavaScript 语言本身（含 ECMAScript 与常见内置对象），不要求读取当前打开的文件。若问题主要是「浏览器如何调度 JS」「V8 如何编译执行」「DevTools 怎么观察」，切换到对应浏览器/V8/DevTools 规则。

用户讨论 **JavaScript 语法、API、原理、边界行为** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅下列权威站点核对语义后再组织回答。**不引用 TC39 ECMA-262 全文**（难读）；边界语义用 MDN + javascript.info 说明即可。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 MDN 对应页面，核对关键结论（语义、边界行为、示例是否与文档一致）。概念教学或需要更系统讲解时再补查 javascript.info；API/语句语义以 MDN 为准。
2. **再对照仓库 demo**：若 `apps/javascript` 有同主题文件，用于对齐示例风格与本地演示；**demo 不能替代第 1 步**，若与 MDN 冲突以 MDN 为准并注明。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、版本/兼容性或边界行为附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 MDN / javascript.info」；仅引用本仓库 demo 中已标注权威链接的内容，不补充未核对的细节。

## 来源优先级（冲突时：以 MDN 为准，javascript.info 作补充讲解）

| 用途 | 站点 | 入口 |
|------|------|------|
| API 用法、语句语义、内置对象 | MDN | https://developer.mozilla.org/en-US/docs/Web/JavaScript |
| 概念、原理、教学向解释 | JavaScript.info | https://javascript.info |

## 回答要求

1. **先定性再举例**：一句话说明「文档怎么说」，再给最小可运行示例（与 `apps/javascript` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、版本/兼容性或边界行为附 Markdown 链接（MDN 具体页面或章节锚点；必要时 javascript.info 对应章节）；链接须为本次已查阅页面，不得只贴首页或无关页。
3. **查不到或文档未写明**：明确写「文档未覆盖 / 实现相关」，不编造；可说明引擎差异并标注为实现细节。
4. **与仓库 demo 对齐**：若 `apps/javascript` 有同主题文件，可提示用户打开对应 demo，不重复贴大段已有内容。

## 禁止

- **未查 MDN 就给出 JavaScript 语义/API 关键结论**（仓库 demo 或「常识」不算已核对）；概念教学需要系统讲解时再补查 javascript.info。
- 只给「众所周知」式总结且无链接。
- 把博客、知乎、未注明版本的二手教程当作首要依据。
- 主动贴 ECMA-262 条款号或 tc39.es 长文（用户未明确要求时不引用）。

## 用户可追加的提问模板

> 请按项目规则：以 MDN 为准回答，必要时补充 javascript.info，附链接与最小示例。问题：……
