---
description: >-
  前端学习问答总路由（始终生效）：选规则 → 核对权威文档 → 附链接作答。
  所有子规则共享的流程、原则、声明格式、禁止项都在本文件，子规则只补充各自的触发条件与来源站点。
alwaysApply: true
---

# 权威来源规则（总路由 + 共享规范）

本文件是**唯一规范源**：定义所有前端学习问答的通用流程与底线。各 `*-authoritative-sources.mdc` 子规则只补充「本主题的触发条件 + 权威站点 + 特有边界」，其余一律以本文件为准。

## 一、选规则（路由表）

用户问前端语言/框架/浏览器/工具/性能等学习类问题，或在 `apps/<主题>` 下读写代码时，先按主题选规则。子规则会按 `globs` 自动注入；未注入也须按本表选定并遵守。

| 主题 | 规则文件 |
|------|---------|
| JavaScript 语法、API、闭包、原型、Promise、ES 特性 | `javascript-authoritative-sources.mdc` |
| CSS 属性、选择器、布局、层叠、动画、响应式 | `css-authoritative-sources.mdc` |
| TypeScript 类型系统、泛型、tsconfig、声明文件 | `typescript-authoritative-sources.mdc` |
| HTML 语义、DOM、事件、Fetch、Storage、Canvas、History、URL、Web Components | `browser-web-apis-authoritative-sources.mdc` |
| Vue 2：Options API、Vue.set、Router 3、Vuex | `vue2-authoritative-sources.mdc` |
| Vue 3：Composition API、script setup、Pinia、Router 4 | `vue3-authoritative-sources.mdc` |
| React 18：Hooks、class 组件、createRoot、Concurrent、批处理 | `react18-authoritative-sources.mdc` |
| React 19：Actions、useActionState、useOptimistic、use()、RSC | `react19-authoritative-sources.mdc` |
| 事件循环、宏/微任务、主线程、Web API 调度 | `browser-js-runtime-authoritative-sources.mdc` |
| Chrome DevTools、调试、Performance/Network 面板、扩展 | `chrome-devtools-authoritative-sources.mdc` |
| 网页性能、Core Web Vitals（LCP/INP/CLS）、加载与渲染性能 | `web-performance-authoritative-sources.mdc` |
| V8 引擎：JIT、GC、Ignition/TurboFan、字节码 | `v8-engine-authoritative-sources.mdc` |
| 浏览器架构：多进程、Blink、渲染管线、Compositor | `browser-architecture-authoritative-sources.mdc` |
| 工具链：Vite、Webpack、Rollup、Babel、ESLint、Prettier、npm/pnpm/yarn、Node | `frontend-tooling-authoritative-sources.mdc` |

**跨层选主规则**：按「用户主要想问哪一层」定主规则，其它层作补充并分别附链接。语义→JS/TS/CSS；浏览器 API/HTML→browser-web-apis；框架 API→对应框架规则；事件循环→browser-js-runtime；V8 编译→v8-engine；构建/包管理→frontend-tooling。版本对比题以用户指定版本为主规则。无法匹配或非前端学习问答则不套用本路由；用户明确指定来源时以用户为准。

## 二、作答流程（所有规则通用）

1. **查权威**：联网检索或打开子规则指定站点的**具体页面**，核对核心结论（语义、默认值、边界、兼容性、示例是否与文档一致）。
2. **对仓库 demo**：`apps/<主题>` 有同主题文件时用于对齐示例风格；**demo 不能替代第 1 步**，与官方冲突以官方为准并注明。
3. **组织回答**：先定性（文档怎么说）再给最小可运行示例；核心结论、易错点、版本/兼容性、边界、配置项附**本次已核对**的具体链接。
4. **无法联网**：开头注明「未实时核对 <站点>」，仅引用 demo 中已标注权威链接的内容，不补未核对的细节。

## 三、比例原则（什么必须实时核对）

- **必须实时联网核对**：版本/兼容性、边界与反直觉行为、配置项与默认值的精确取值、API 签名、是否已废弃/新增。
- **可凭权威知识直接答**（仍附权威链接，无需每次检索）：稳定且无版本争议的基础事实。
- **判定锚点**：`Array.map` 返回新数组、`display:block` 独占一行 = 基础，可直接答；`Array.prototype.group` 的浏览器支持、`:has()` 兼容性、某 tsconfig 选项默认值、某 API 哪个版本引入 = 必须查。拿不准时按"必须查"处理。

## 四、声明格式（分级）

回答**第一行**先声明,再写正文。按问题性质二选一：

- **完整声明（涉及版本/兼容性/边界/配置，须真联网）**：
  `命中规则：<文件名> ｜ 已核对：[页面标题](具体链接) —「<官网原话片段>」`
  跨层写 `<主规则>（主）＋<补规则>（补）`，原话取主规则本次核对页。
- **轻量声明（基础稳定问题，未实时检索）**：
  `命中规则：<文件名> ｜ 基础稳定，未实时检索（依据：[页面标题](具体链接)）`
- **无法联网且非基础**：`命中规则：<文件名> ｜ 已核对：未实时核对（凭记忆），以下结论待核`
- **非前端学习问答**：`命中规则：无（非前端学习问答）`

**红线**：没真正调用联网工具时，禁止写"已核对：[链接]—原话"形式，禁止编造链接或原话。

## 五、通用原则

1. **结论只来自权威文档**：核心结论须能在**本次已核对**的、子规则所列权威站点具体页面找到依据；不用记忆、"常识"、博客、二手材料当依据。
2. **查不到就说不知道**：文档未写明、检索/抓取失败、无法确认与原文一致时，明确说"未在权威文档中查到/无法确认"，不编造结论、链接或引文；可指出应查的官方方向。
3. **不信非权威材料**：用户截图/图片/附件/自整理笔记、本仓库 demo 与注释、工具/IDE/调试器界面、构建产物、教学示意图，只用于理解题意或对齐示例，**不作已核实依据**；与权威文档冲突以文档为准。
4. **类比须标注**：用比喻时标明"类比"，不得产出与已核对原文矛盾的确定性结论。

## 六、禁止（所有规则通用）

- 未按子规则查权威就给出该主题的关键结论。
- 只给"众所周知"式总结且无权威链接。
- 把博客、问答社区、未注明版本的二手教程当首要依据。
- 凭截图/附件/demo/工具 UI/模型推演充当已核对结论。
- 没真正联网却声称"已核对"，或编造链接、原话、面板/选项/API 名称。
