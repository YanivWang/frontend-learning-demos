---
description: >-
  HTML、DOM、表单、事件、Fetch、Storage、Canvas、History、URL、Web Components 等浏览器 Web API。
  以 MDN 为准，标准边界查 WHATWG/W3C。通用流程/原则/声明格式见 authoritative-sources-router.mdc。
globs: apps/javascript/06-浏览器API/**
alwaysApply: false
---

# HTML / DOM / Web API（权威来源）

**触发**：用户问浏览器提供给页面的 API 或 HTML 语义（DOM、事件、Fetch、Storage、Canvas、表单、History、URL、Web Components）。

**来源**（冲突时：日常用法以 MDN 为准，标准边界以 WHATWG/W3C 为准）：

| 用途 | 站点 | 入口 |
|------|------|------|
| HTML、DOM、Web API 用法与兼容性 | MDN | https://developer.mozilla.org/en-US/docs/Web |
| HTML 标准、事件循环术语 | WHATWG HTML | https://html.spec.whatwg.org/ |
| DOM 标准 | WHATWG DOM | https://dom.spec.whatwg.org/ |
| 特定 Web API 规范 | W3C | https://www.w3.org/TR/ |

**本主题特有**：

- 区分「浏览器 API 行为」与「ECMAScript 语言语义」（后者 → javascript 规则）。
- 相邻层：事件循环/任务队列 → browser-js-runtime；DevTools 操作 → chrome-devtools。

> 流程、比例原则、分级声明、通用原则与禁止：见 `authoritative-sources-router.mdc`。
