---
description: >-
  用户提问 CSS 语法、属性、布局、层叠、动画、响应式等（聊天问答或编辑相应代码时均适用）。
  必须先查 MDN 核对后再答，规范边界查 W3C，现代实践查 web.dev，附具体链接；不用于 JavaScript/Vue/React 等非 CSS 话题。
globs: apps/css/**
alwaysApply: false
---

# CSS 学习问答（权威来源）

**触发**：用户问的是 CSS 本身（属性、选择器、布局、层叠、动画、响应式、现代特性等），不要求读取当前打开的文件。

用户讨论 **CSS 语法、属性用法、布局原理、层叠与优先级、浏览器渲染行为** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅下列权威站点核对语义后再组织回答。**不主动贴 W3C 规范全文**（难读）；日常属性用法以 MDN 为准，规范级定义需要时再引用 W3C 模块页。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 MDN 对应页面，核对关键结论（属性语义、默认值、适用元素、浏览器支持、示例是否与文档一致）。规范边界再查 W3C 模块，现代实践/性能/可访问性建议再查 web.dev。
2. **再对照仓库 demo**：若 `apps/css` 有同主题文件，用于对齐示例风格与本地演示；**demo 不能替代第 1 步**，若与 MDN 冲突以 MDN 为准并注明。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、兼容性或规范边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 MDN / W3C / web.dev」；仅引用本仓库 demo 中已标注权威链接的内容，不补充未核对的细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：属性用法以 MDN 为准，规范定义以 W3C 为准，现代实践以 web.dev 补充）

| 用途 | 站点 | 入口 |
|------|------|------|
| 属性用法、选择器、@规则、浏览器行为 | MDN | https://developer.mozilla.org/en-US/docs/Web/CSS |
| 规范级定义、语法形式、模块边界 | W3C CSS 规范 | https://www.w3.org/TR/CSS |
| 现代 CSS 实践、布局技巧、性能与可访问性 | web.dev | https://web.dev/learn/css |

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：css-authoritative-sources.mdc ｜ 已核对：[页面标题](MDN/W3C/web.dev 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定性再举例**：一句话说明「文档怎么说」（含默认值、继承性、适用场景），再给最小可运行示例（与 `apps/css` demo 风格一致）。
2. **必须标注出处**：核心结论、易错点、兼容性或规范边界附 Markdown 链接（MDN 具体属性页或模块页；必要时 W3C 对应模块、web.dev 对应章节）；链接须为本次已查阅页面，不得只贴首页或无关页。
3. **查不到或文档未写明**：明确写「文档未覆盖 / 实现相关」，不编造；可说明浏览器差异并标注为实现细节。
4. **与仓库 demo 对齐**：若 `apps/css` 有同主题文件，可提示用户打开对应 demo，不重复贴大段已有内容。

## 禁止

- **未查 MDN 就给出 CSS 属性/布局/层叠关键结论**（仓库 demo 或「常识」不算已核对）；规范边界再查 W3C，现代实践再查 web.dev。
- 只给「众所周知」式总结且无链接。
- 把博客、知乎、未注明版本的二手教程当作首要依据。
- 主动贴 W3C 规范长文或大量条款号（用户未明确要求时不引用）。

## 用户可追加的提问模板

> 请按项目规则：以 MDN 为准回答，规范边界补查 W3C，现代实践补查 web.dev，附链接与最小示例。问题：……
