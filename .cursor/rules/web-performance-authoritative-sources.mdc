---
description: >-
  用户问网页性能优化、Core Web Vitals（LCP/INP/CLS/FCP/TTFB）、加载策略、渲染性能、资源优先级、图片/字体优化（聊天问答或编辑相应代码时均适用）。
  必须先查 web.dev 核对后再答，附链接；DevTools 操作细节见 chrome-devtools 规则。
globs: apps/css/06-性能/**
alwaysApply: false
---

# 网页性能与 Core Web Vitals（权威来源）

**触发**：用户问的是 **Web 性能测量、优化手段、Core Web Vitals 指标**，不要求读取当前打开的文件。

用户讨论 **LCP、INP、CLS、加载瀑布、关键渲染路径、懒加载、预连接、减少 JS/CSS 阻塞** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅 web.dev 核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：用联网检索或打开 web.dev 对应页面，核对指标定义、阈值、推荐优化是否与当前文档一致。
2. **再对照仓库 demo**：若 `apps/css/06-性能`、`apps/javascript/06-浏览器API/Performance-API.html` 等有同主题文件，用于对齐示例；**demo 不能替代第 1 步**。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、指标阈值、优化优先级或工具边界附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 web.dev」；不补充未核对的阈值或 API 细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：以 web.dev 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| Core Web Vitals、性能指南、最佳实践 | web.dev | https://web.dev |
| 性能相关 Web API 语义（补充） | MDN | https://developer.mozilla.org/en-US/docs/Web/API/Performance |

## 路由通用原则

须同时遵守 `authoritative-sources-router.mdc`「通用原则」：核心结论仅来自本次核对的权威文档；未查到则说明不知道、不编造；用户截图/图片/附件及仓库 demo 仅作题意与示例、不作已核实依据；不得用工具 UI 或未核对材料反推规范。

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：web-performance-authoritative-sources.mdc ｜ 已核对：[页面标题](web.dev 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定义指标再讲优化**：说明 web.dev 对指标的定义与「好/需改进/差」阈值，再给可执行优化清单。
2. **必须标注出处**：核心结论、指标阈值、优化优先级或工具边界附 Markdown 链接（web.dev 具体文章；必要时 MDN Performance API 页）；不得只贴首页。
3. **DevTools 交叉引用**：说明「如何在 Chrome 中测量」时，可引用 developer.chrome.com，但以 web.dev 的指标定义为主。
4. **查不到或文档未写明**：按「路由通用原则」说明不知道，**不编造**；可注明工具版本相关。

## 禁止

- **未查 web.dev 就给出 CWV 阈值或优化结论**。
- 把过时博客中的指标阈值（如旧版 FID 说法未注明）当作当前标准。
- 只给 checklist 且无链接。
- **凭用户上传的截图/图片/附件、仓库 demo、工具 UI 或模型推演充当已核对结论**（见「路由通用原则」）。

## 用户可追加的提问模板

> 请按项目规则：以 web.dev 为准回答性能/CWV 问题，附链接与优化建议。问题：……
