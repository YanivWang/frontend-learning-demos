---
description: >-
  用户问 HTML、DOM、表单、事件、Fetch、Storage、Canvas、History、URL、Web Components 等浏览器 Web API（聊天问答或编辑相应代码时均适用）。
  必须先查 MDN 核对后再答，涉及标准边界再查 WHATWG/W3C；不用于 ECMAScript 语义、事件循环或 DevTools 操作。
globs: apps/javascript/06-浏览器API/**
alwaysApply: false
---

# HTML / DOM / Web API 学习问答（权威来源）

**触发**：用户问的是 **浏览器提供给页面的 API 或 HTML 语义**，包括 DOM、事件模型、Fetch、Storage、Canvas、表单、History、URL、Web Components 等；不要求读取当前打开的文件。

用户讨论 **HTML 元素语义、DOM API、浏览器事件、Web API 用法与兼容性** 时，**不得仅凭记忆或仓库 demo 直接作答**；须先查阅下列权威站点核对后再组织回答。

## 作答流程（必须按序执行）

1. **先查权威网站**：常规用法、兼容性、示例优先查 MDN 具体页面；涉及标准术语、算法或争议边界时再查 WHATWG HTML / DOM Standard 或对应 W3C 规范。
2. **再对照仓库 demo**：若 `apps/javascript/06-浏览器API` 或相关 HTML demo 有同主题文件，用于对齐示例；**demo 不能替代第 1 步**，若与文档冲突以官方文档为准。
3. **最后组织回答**：按下方「回答要求」输出；核心结论、易错点、兼容性或版本相关结论附已核对过的具体链接。
4. **无法联网时**：在回答开头明确写「未能实时核对 MDN / Web 标准」；不补充未核对的 API 细节。

> **比例原则**：极基础且稳定的事实（基本语法、默认行为等）可直接作答并附权威链接，无需每次实时检索；凡涉及版本/兼容性/边界行为或配置项，必须按上面流程实时核对。联网默认先尝试，确实不可用时才走「无法联网」降级。

## 来源优先级（冲突时：日常用法以 MDN 为准，标准边界以 WHATWG/W3C 为准）

| 用途 | 站点 | 入口 |
|------|------|------|
| HTML、DOM、Web API 用法与兼容性 | MDN | https://developer.mozilla.org/en-US/docs/Web |
| HTML 标准、事件循环相关标准术语 | WHATWG HTML | https://html.spec.whatwg.org/ |
| DOM 标准 | WHATWG DOM | https://dom.spec.whatwg.org/ |
| W3C 维护的特定 Web API 规范 | W3C Standards | https://www.w3.org/TR/ |

## 回答要求

**第 0 步｜先声明命中规则与来源（须真调用联网工具）**：本规则结论须先**实际发起一次联网检索/抓取**核对；回答开头先输出一行 `命中规则：browser-web-apis-authoritative-sources.mdc ｜ 已核对：[页面标题](MDN/WHATWG/W3C 具体链接) —「<官网原话片段>」`。未真正联网核对时只能写 `已核对：未实时核对（凭记忆）`，且不得编造链接或原话。再进入下列步骤。

1. **先定性再举例**：说明 MDN/标准文档对 API 或元素语义的定义，再给最小 HTML/JS 示例。
2. **必须标注出处**：核心结论、易错点、兼容性或标准边界附 Markdown 链接（MDN 具体页；必要时 WHATWG/W3C 具体章节）；不得只贴首页。
3. **相邻层区分**：纯 JS 语言语义 → javascript 规则；事件循环/任务队列 → browser-js-runtime；DevTools 操作 → chrome-devtools。
4. **查不到或文档未写明**：明确写「文档未覆盖 / 浏览器实现相关」，不编造。

## 禁止

- **未查 MDN 或对应 Web 标准就给出 HTML/DOM/Web API 关键结论**。
- 把博客、兼容性不明的教程或框架封装文档当作浏览器 API 的首要依据。
- 混淆浏览器 API 行为与 ECMAScript 语言语义。

## 用户可追加的提问模板

> 请按项目规则：以 MDN / Web 标准为准回答 HTML/DOM/Web API 问题，附链接与最小示例。问题：……
